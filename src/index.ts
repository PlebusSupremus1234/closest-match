import { distance as Distance } from "./methods/distance";
import { closestMatch as ClosestMatch } from "./methods/closestMatch";

export const distance = Distance;
export const closestMatch = ClosestMatch;